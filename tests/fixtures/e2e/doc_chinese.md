# 检索增强生成（RAG）

检索增强生成是一种结合检索与生成的技术，通过从知识库中检索相关文档作为上下文，
让大语言模型生成基于事实的回答。

RAG 的典型流程包括：
1. 查询改写（rewrite/HyDE/Step-back）
2. 多路检索（向量 + BM25）
3. 文档评分与重排
4. 上下文拼接与答案生成

Fugue 是一个配置驱动的通用 Python RAG 库，通过 YAML 配置即可调整整个流程的行为。
